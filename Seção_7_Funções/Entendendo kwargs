"""
Entendendo **kwargs

def cores_favoritas(**kwargs):
    for pessoa, cor in kwargs.items():
        print(f'A cor favorita de {pessoa.title()} é {cor}')

cores_favoritas(marcos='Verde', fernando='Azul', vanessa='Rosa')

# Nem os parâmetros *args e **kwargs não são obrigatórios

cores_favoritas()
cores_favoritas(geek='Navy')

#Exemplo mais complexo
def comprimento_especial(**kwargs): #**kawargs é um dicionário
    if 'geek' in kwargs and kwargs['geek'] == 'Python':
        return 'Você recebeu um cumprimento Pythonico Geek!'
    elif 'geek' in kwargs:
        return f'{kwargs['geek']} Geek!'
    return 'Não sei quem você é...'

print(comprimento_especial())
print(comprimento_especial(geek='Python'))
print(comprimento_especial(geek='Oi'))
print(comprimento_especial(geek='especial'))
print(comprimento_especial())

→Nas nossas funções, podemos ter (nesta ordem):

1. Parâmetros obrigatórios;
2. *args;
3. Parâmetros default (Não obrigatórios);
4. **kwargs


#Estrutura
def minha_funcao(idade, nome, *args, solteiro=False, **kwargs):
    print(f'{nome} tem {idade} anos')
    print(args)
    if solteiro:
        print('Solteiro')
    else:
        print('Casado')
    print(kwargs)

minha_funcao(8,'Carlos') #idade e nome
minha_funcao(18,'Felicity', 4,5,3, solteiro=True) #idade, nome, args e default
minha_funcao(33, 'Felipe', eu='Não', você='vai') #idade, nome e kwargs
minha_funcao(18,'Carla', 9, 4, 3, java=False, python=True) #idade, nome, args e kwargs


# Função com a ordem correta dos parâmetros
def mostra_info(a, b, *args, instrutor='Geek', **kwargs):
    return [a, b, args, instrutor, kwargs]


print(mostra_info(1,2,3, sobrenome='University', cargo='instrutor'))



# Função com a ordem incorreta de parâmetros
def mostra_info(a, b, instrutor='Geek', *args, **kwargs):
    return [a, b, args, instrutor, kwargs]


print(mostra_info(1,2,3, sobrenome='University', cargo='instrutor'))


#Uso do “*” para Desempacotar
def mostra_nomes(**kwargs):
    return f'{kwargs['nome']} {kwargs['sobrenome']}'

nomes = {'nome':'Felicity', 'sobrenome':'Jones'}

#Outra alternativa do dicionário
##nomes = dict(nome='Felicity', sobrenome='Jones')

# print(mostra_nomes(nomes)) -> Não funciona
print(mostra_nomes(**nomes))
"""

def soma_multiplos_numeros(a,b,c, **kwargs):
    print(a+b+c)

lista = [2,3,4]
tupla = (2,3,4)
conj = {2,3,4}

#OU -> soma_multiplos_numeros(2,3,4)

soma_multiplos_numeros(*lista)
soma_multiplos_numeros(*tupla)
soma_multiplos_numeros(*conj)

dicionario = dict(a=2,b=3,c=4)
soma_multiplos_numeros(**dicionario)


dicionario = dict(a=2,b=3,c=4, nome='Geek')
soma_multiplos_numeros(**dicionario, lang='Python')

