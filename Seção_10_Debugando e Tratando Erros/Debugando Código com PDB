"""
→ Debugando Código com PDB
#Prática ruim
def dividir(a, b):
    print(a,b)
    try:
        return int(a) / int(b)
    except (ValueError, ZeroDivisionError) as err:
        return f"Erro! {err}"

print(dividir(4,7))
"""

def dividir(a, b):
    try:
        return int(a) / int(b)
    except (ValueError, ZeroDivisionError) as err:
        return f"Erro! {err}"
    
print(dividir(4,7))