nome = "Gustavo Kovacs Gerlach"
lista = [1, 3, 5, 7, 9]
numeros = range(1, 10)  # Tem que transformar em lista


# enumerate(nome)
##((0, 'G'), (1, 'u'), (2, 's'), (n+1, 'next letter')') -> Tupla
"""
for indice, letra in enumerate(nome):
    print(nome[indice])

for indice, letra in enumerate(nome):
    print(letra)

for _, letra in enumerate(nome):
    print(letra)

    ## Trazer apenas os números
for valor in enumerate(nome):
    print(valor[0])

## Trazer apenas as letras
for valor in enumerate(nome):
    print(valor[1])

"""
